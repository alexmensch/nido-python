version: "3.4"
services:
  nido-api:
    build:
      context: .
      target: nido-api
    ports:
      - "80:80"
    environment:
      - FLASK_APP=nido
      - FLASK_DEBUG=TRUE
      - FLASK_ENV=development
      - FLASK_RUN_HOST=0.0.0.0
      - FLASK_RUN_PORT=80
      - NIDO_DEBUG=""
      - NIDOD_RPC_HOST=nido-supervisor
      - NIDOD_MQTT_HOSTNAME=mosquitto
    volumes:
      - ./nido-api/instance:/app/instance
      - ./nido-api/nido:/app/nido
    entrypoint:
      - flask
      - run
      - --without-threads
  nido-supervisor:
    build:
      context: .
      target: nido-supervisor
    volumes:
      - ./nido-supervisor/instance:/app/instance
      - ./nido-supervisor/log:/app/log
      - ./nido-supervisor/nidod:/app/nidod
  mosquitto:
    build:
      context: ./mqtt
      target: nido-mqtt
    ports:
      - "1883:1883"
  homebridge:
    image: oznu/homebridge:latest
    restart: always
    volumes:
      - ./homebridge:/homebridge
    environment:
      - TZ=America/Los_Angeles
      - HOMEBRIDGE_CONFIG_UI=1
      - HOMEBRIDGE_CONFIG_UI_PORT=8080
    ports:
      - "8080:8080"
